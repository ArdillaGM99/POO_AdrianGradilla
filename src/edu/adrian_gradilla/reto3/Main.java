package edu.adrian_gradilla.reto3;
import edu.adrian_gradilla.reto3.Ui.CLI;

public class Main {
    public static void main(String[] args) {
        /* Mando llamar la clase CLI y el método showMenu para mostrar el menú principal. */
        CLI cli = new CLI();
        cli.showMenu();
    }
}
